// import React from 'react';
// import FormInput from '../../common/FormInput';
// import ConnectionId from '../PersoInfos/ConnectionId';

export default class {
/*
	render = () => (
		<div id="logIn">
			<fieldset>
				<legend>Connexion</legend>
				<form onSubmit={() => {}}>
					<ConnectionId onChange={() => {}} />
					<FormInput
						label="Mot de passe :"
						type="password"
						onChange={() => {}}
						onBlur={() => {}}
					/>
					<FormInput
						label="Confirmation du mot de passe :"
						type="password"
						onChange={() => {}}
						onBlur={() => {}}
					/>
					<FormInput label="Prénom :" onChange={() => {}} />
					<FormInput label="Nom :" onChange={() => {}} />
					<FormInput label="Email :" onChange={() => {}} />
					<FormInput label="Adresse :" onChange={() => {}} />
					<FormInput label="Phone :" onChange={() => {}} />
					<FormInput label="Logo :" onChange={() => {}} />
				</form>
			</fieldset>
		</div>
	);
*/
}
